# 😃, get off final stand

```lua
local mainAPI = loadstring(game:HttpGet("https://raw.githubusercontent.com/ProphecySkondo/finalstand/refs/heads/main/apis/main.lua"))()
```
****
* **What's inside this shit**
```lua
mainAPI:SpeedReset() --// simple hard reset (im new to coding fuck you)
mainAPI:mainAPI.Load(wat: bool) --// some shit just loads the game in for u
mainAPI.Output(thing: string) --// prints
mainAPI:ActivateNoSlow() --// start no slow
mainAPI:ChangeSlot(Slot: number) --// change slot
mainAPI:TweenTo(targetCFrame: CFrame, tweenTime: number) --// tween
mainAPI:StartTouchFling(flingpower: number) --// fling
```

```lua
local mainAPI = loadstring(game:HttpGet("https://raw.githubusercontent.com/ProphecySkondo/finalstand/refs/heads/main/apis/main.lua"))()
mainAPI:SpeedReset() --// simple hard reset (im new to coding fuck you)
mainAPI:mainAPI.Load(wat: bool) --// some shit just loads the game in for u
mainAPI.Output(thing: string) --// prints
mainAPI:ActivateNoSlow() --// start no slow
mainAPI:ChangeSlot(Slot: number) --// change slot
mainAPI:TweenTo(targetCFrame: CFrame, tweenTime: number) --// tween
mainAPI:StartTouchFling(flingpower: number) --// fling
```

****

*idk, go fuck yourself*
